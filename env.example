# ============================================
# Cấu hình Kết nối Backend
# ============================================
# Copy file này thành .env.local và điền thông tin backend của bạn
# 
# Lệnh: cp env.example .env.local

# ============================================
# CÁCH DỄ NHẤT - DÙNG URL ĐẦY ĐỦ (Khuyến nghị cho deploy)
# ============================================
# Chỉ cần thay đổi số cổng trong URL
# Ví dụ: http://127.0.0.1:8008, http://127.0.0.1:8009, http://127.0.0.1:8020
BACKEND_INTERNAL_URL=http://127.0.0.1:5000

# ============================================
# CÁCH 2 - CHỈ CẦN CỔNG (Tùy chọn)
# ============================================
# Chỉ cần set BACKEND_PORT, hệ thống tự động dùng 127.0.0.1 và http
# BACKEND_PORT=5000

# ============================================
# CÁCH 1: Sử dụng URL đầy đủ
# ============================================
# BACKEND_INTERNAL_URL=http://localhost:5000
# BACKEND_INTERNAL_URL=http://127.0.0.1:8008

# ============================================
# CÁCH 2: Tách riêng HOST và PORT (Tùy chọn)
# ============================================
# BACKEND_HOST=127.0.0.1
# BACKEND_PORT=5000
# BACKEND_PROTOCOL=http

# ============================================
# Hướng dẫn:
# ============================================
# 1. Development (Local) - Backend chạy trên cổng 5000:
#    BACKEND_INTERNAL_URL=http://localhost:5000
#
# 2. Production trên cùng server - Backend chạy trên cổng khác (ví dụ: 8080):
#    BACKEND_INTERNAL_URL=http://localhost:8080
#
# 3. Production với Docker:
#    BACKEND_INTERNAL_URL=http://backend:5000  (tên service trong docker-compose)
#
# 4. Production với domain:
#    BACKEND_INTERNAL_URL=https://api.yourdomain.com
#
# 5. Sử dụng cổng khác - CÁCH DỄ NHẤT (chỉ cần đổi số cổng):
#    BACKEND_INTERNAL_URL=http://127.0.0.1:8008
#    BACKEND_INTERNAL_URL=http://127.0.0.1:8009
#    BACKEND_INTERNAL_URL=http://127.0.0.1:8020
#
# 6. Hoặc chỉ dùng số cổng (tùy chọn):
#    BACKEND_PORT=8008
#    BACKEND_PORT=8009
#    BACKEND_PORT=8020
#    (Tự động kết nối đến http://127.0.0.1:xxxx)
#
# 7. Hoặc tách riêng HOST và PORT:
#    BACKEND_HOST=localhost
#    BACKEND_PORT=8080
#    BACKEND_PROTOCOL=http

